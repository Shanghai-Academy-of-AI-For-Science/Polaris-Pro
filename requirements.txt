# ============================================================================
# Polaris-Pro inference dependencies (pinned to the versions verified working)
# Python 3.10.  Install torch FIRST (it carries the CUDA runtime), then the rest.
# ============================================================================

# --- Core (install first; CUDA 12.4 build) ---
# torch / torchvision must match the CUDA on the deploy host.
# These wheels are cu124; if the host driver differs, install the matching
# torch 2.6.0 build from https://download.pytorch.org/whl/ .
torch==2.6.0
torchvision==0.21.0

# --- Transformers stack (EXACT pins — required for correctness) ---
# transformers 5.0.0 provides Sam3Model (med_seg) and the qwen3_vl arch used here.
# Do NOT upgrade to 5.1.x (DeepSpeed/backward regression) or downgrade.
transformers==5.0.0
tokenizers==0.22.2
accelerate==1.7.0
safetensors==0.5.3
huggingface_hub==1.18.0

# --- Numerics ---
numpy==2.2.6
scipy==1.15.3
einops==0.8.2

# --- Modality-specific ---
# Molecule: RDKit (SMILES↔graph) + PyTorch-Geometric (GNN encoder)
rdkit==2026.3.2
torch-geometric==2.7.0
# Medical-image segmentation (med_seg): OpenCV for image ops
opencv-python-headless==4.13.0.92
# Weather: netCDF / zarr I/O for ERA5-format gridded fields
# (xarray needs the `zarr` package to read .zarr stores, incl. assets/era5_stats)
netCDF4==1.7.4
xarray==2025.6.1
zarr==2.18.3

# --- Attention kernel (OPTIONAL) ---
# flash-attn speeds up attention but must be COMPILED against the installed
# torch/CUDA (PyPI ships only an sdist, so `pip install` builds from source and
# can fail on machines without the CUDA toolkit). It is intentionally left OUT
# of the default install: without it, the model auto-falls back to eager
# attention (identical outputs, only slower). To enable it, install manually:
#   pip install flash-attn==2.7.4.post1 --no-build-isolation
