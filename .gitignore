# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# The model directory (weights, config, tokenizer, model card) lives on the
# Hugging Face Hub, not in this code repo.
model/

# Local verification / build artifacts (not part of the release).
baseline/
scripts/

# Env
.venv/
venv/
*.log
