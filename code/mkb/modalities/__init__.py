"""
Per-modality implementations.

Each sub-package (rna, protein, mol, vision, …) provides encoder,
projector, decoder, and processor classes that are registered with the
ComponentRegistry on import.
"""
